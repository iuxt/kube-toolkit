apiVersion: v1
kind: Pod
metadata:
  labels:
    run: kube-toolkit
  name: kube-toolkit
  namespace: public
spec:
  containers:
  - image: registry.cn-hangzhou.aliyuncs.com/iuxt/kube-toolkit:1
    name: kube-toolkit
    command:
    - "sleep"
    - "infinity"

